version: '3.8'

# Simplified version without strict health check dependencies
# Use this if you're having issues with the main docker-compose.yml

services:
  # LocalStack - AWS service emulation
  localstack:
    image: localstack/localstack:latest
    container_name: localstack
    ports:
      - "4566:4566"
      - "4510-4559:4510-4559"
    environment:
      - SERVICES=ecs,ec2,ecr,elbv2,ssm,secretsmanager,logs,iam,sts
      - DEBUG=1
      - DOCKER_HOST=unix:///var/run/docker.sock
      - LOCALSTACK_HOST=localstack
      - DATA_DIR=/tmp/localstack/data
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "./localstack-init:/etc/localstack/init/ready.d"
      - "localstack-data:/tmp/localstack"
    networks:
      - ecs-network

  # Sample Application - starts regardless of LocalStack health
  sample-app:
    build: ./sample-app
    container_name: sample-app
    ports:
      - "8080:8080"
    environment:
      - AWS_DEFAULT_REGION=us-east-1
      - AWS_ACCESS_KEY_ID=test
      - AWS_SECRET_ACCESS_KEY=test
      - AWS_ENDPOINT_URL=http://localstack:4566
      - LOCALSTACK_HOSTNAME=localstack
    networks:
      - ecs-network
    restart: on-failure

  # Jenkins - For testing deploy pipelines
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins
    ports:
      - "8081:8080"
      - "50000:50000"
    environment:
      - JAVA_OPTS=-Djenkins.install.runSetupWizard=false
    volumes:
      - jenkins-data:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
      - ./jenkins/pipelines:/var/jenkins_home/pipelines
    networks:
      - ecs-network

  # AWS CLI container for manual testing
  aws-cli:
    image: amazon/aws-cli:latest
    container_name: aws-cli
    environment:
      - AWS_DEFAULT_REGION=us-east-1
      - AWS_ACCESS_KEY_ID=test
      - AWS_SECRET_ACCESS_KEY=test
      - AWS_ENDPOINT_URL=http://localstack:4566
    entrypoint: ["/bin/sh", "-c", "sleep infinity"]
    networks:
      - ecs-network

networks:
  ecs-network:
    driver: bridge

volumes:
  localstack-data:
  jenkins-data:
